.message
	.message__title
		%p.message__title__username= message.user.name
		%p.message__title__timestamp= message.created_at.strftime("%Y/%m/%d(%a) %H:%M:%S")
	- if message.text?
		%p.message__text= message.text
	- if message.image?
		= image_tag message.image, class: "message__image"